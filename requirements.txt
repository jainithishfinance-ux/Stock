# StockVision Pro — Python Dependencies (Vercel-optimized)
# Install with: pip install -r requirements.txt

flask==3.0.0
flask-cors==4.0.0

# Data
yfinance==0.2.36
pandas==2.1.4
numpy==1.26.3

# ML (sklearn only — keeps bundle under 500 MB)
scikit-learn==1.4.0
# xgboost removed for Vercel size limit — model.py falls back to sklearn

# Utilities
requests==2.31.0
python-dotenv==1.0.0
